let bisiesto =(ano) => {
    let anoCopia=parseInt(ano)
    let cadena=toString(ano)
    let copia=""
    let boolean=false
    while (boolean===false){
        if (cadena.lastIndexOf==="0"){
            anoCopia=anoCopia/10
        }
        else{
            boolean=true
        }
    }


    
}