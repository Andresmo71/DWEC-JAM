
const Footer = ({ total,productos }) => {

  return (
    <footer>
        <div className="container">
            {productos.map}
        </div>
        <p>
            total : {total}
        </p>

    </footer>
  );
};

export default MenuSuperior;